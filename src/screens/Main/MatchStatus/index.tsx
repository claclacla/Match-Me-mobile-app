import { Layout, Text } from '@ui-kitten/components';

import styles from '../../../styles';

const MatchStatusScreen = () => {
    return (
        <Layout style={styles.container}>
            <Text category='h3' style={styles.title}>Match</Text>
        </Layout>
    );
};

export default MatchStatusScreen;